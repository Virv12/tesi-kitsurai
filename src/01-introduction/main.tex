\section{Introduction}
\label{sec:introduction}
In recent years, replicated storage services in the field of Cloud Computing have gained significant importance.
In a context where applications must serve millions of users simultaneously, often geographically distributed,
it becomes crucial to design systems capable of providing horizontal scalability, high availability, and fault tolerance.

These properties must coexist with the need to keep response times low and ensure efficient resource management.
Traditional data management systems based on relational models are often unsuitable for these new distributed scenarios.
For this reason, modern architectures increasingly rely on NoSQL solutions that, in exchange for simpler operations,
offer lower overhead and better scalability compared to traditional SQL databases.

A characterizing feature of Cloud computing is multi-tenancy, where several customers share the same physical or virtual resources.
This brings a number of advantages in terms of flexibility in resources management, but it also introduces additional challenges.
In the absence of explicit isolation mechanisms, a particularly active tenant can compromise the quality of service offered to others,
making precise throughput control for each client essential.
These constraints create a space of hard engineering and algorithmic problems --- from replication protocols to resource managers --- that must be solved jointly.
Building practical, tenant-aware replicated stores thus requires cross-layer designs spanning networking, storage, and control planes.

In these systems,
a key metric that is gaining attention is \emph{data access latency}, as witnessed
by major commercial NoSQL solutions like Amazon \textit{DynamoDB}~\cite{Dynamo2022} or Google \textit{BigTable}~\cite{Bigtable}, that guarantee response times below \qty{10}{\milli\second}. For DynamoDB, the guarantee holds for small requests issued from within the EC2 cloud~\cite{DynamoWhitepaper}. In Bigtable, the guarantee relies on quota and throttling mechanisms that were not part of the original design but introduced later, as
its underlying storage \textit{GFS}~\cite{GFS} also does not make latency one of its main features
focusing on maximizing throughput instead. %%, and is also deployed in multiple clusters to support differentiated applications.

Providing latency and performance guarantees in distributed and multi-tenant environments proves extremely challenging.
%
Indeed, major open-source solutions like \textit{MongoDB}~\cite{Mongo} or \textit{Cassandra}~\cite{Cassandra} adopt a best effort approach,
without guaranteeing precise limits on latency or throughput, especially when multiple clients share resources.
Due to the inability to isolate traffic from multiple tenants, solutions like these are often deployed as separate instances per customer,
shifting resource-management and isolation responsibilities to the underlying cluster scheduler or provisioning layer.

The same fundamental performance problem has been tackled in columnar data stores,
which provide faster access to individual columns when compared with classic relational databases.
Development in the last years has increased due to growing analytical workloads,
and literature~\cite{ColumnarSurvey, ColumnOrientedDatabase} has intensified its focus on storage layouts,
compression and execution techniques that exploit columnar layouts to minimize I/O and CPU overhead for selective and analytical queries.

\paragraph{Paper Contributions}
The Rustikai project is presented as a first attempt to create a key-value store that is not only scalable and resilient,
but also introduces explicit resource control mechanisms to guarantee a minimum throughput to each tenant, even under heavy load conditions.

It achieves these results by leveraging a throughput allocation mechanism and key distribution algorithm, with configurable replication, read concern and write concern per table.
The resulting system is designed to work without the need for replica coordination, making it resilient to faults and network partitions.

The project is implemented in Rust, a language chosen not only for its excellent performance but also for the memory safety model it provides.
Rust is known for delivering performance comparable to C++ while avoiding common classes of errors such as race conditions or memory leaks,
making it particularly suitable for programming complex distributed systems.
In addition, it has an ever-growing set of high quality libraries
that have considerably simplified the development of this project.

\paragraph{Paper Organization}
The paper is organized as follows: Section~\ref{sec:related}
reviews related work and situates our approach within previous research.
Section~\ref{sec:architecture} presents the system's assumptions, API, and overall architecture,
followed by Section~\ref{sec:implementation}, which details our approach and main algorithm.
Section~\ref{sec:results} discusses the experimental results,
highlighting how the system maintains the expected throughput when scaling across multiple machines.
Finally, Section~\ref{sec:conclusions} summarizes the current status of the project
and outlines possible directions for future work.
